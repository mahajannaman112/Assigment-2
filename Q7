def count_inv(A):
    cnt = 0
    n = len(A)
    for i in range(n):
        for j in range(i+1, n):
            if A[i] > A[j]:
                cnt += 1
    return cnt

arr = [2, 4, 1, 3, 5]
print("Inversions:", count_inv(arr))
